name: Release to Maven Repository

on:
  release:
    types: [ published ]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release'
        required: true
        type: string
      repository:
        description: 'Release repository'
        required: true
        type: choice
        options:
          - releases
          - snapshots
        default: 'releases'

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: false

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Determine version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "release" ]; then
            # Extract version from release tag (remove 'v' prefix if present)
            VERSION="${{ github.event.release.tag_name }}"
            VERSION="${VERSION#v}"
          else
            # Use manual input version
            VERSION="${{ github.event.inputs.version }}"
          fi
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          echo "Building version: $VERSION"

      - name: Determine repository and credentials
        id: repository
        run: |
          if [ "${{ github.event_name }}" = "release" ]; then
            REPOSITORY="releases"
          else
            REPOSITORY="${{ github.event.inputs.repository }}"
          fi
          echo "REPOSITORY=$REPOSITORY" >> $GITHUB_OUTPUT
          echo "Publishing to: $REPOSITORY"

      - name: Update version in build.gradle.kts
        run: |
          sed -i 's/project\.version = "[^"]*"/project.version = "${{ steps.version.outputs.VERSION }}"/' ./launcher/build.gradle.kts
          sed -i 's/val isRelease = false/val isRelease = true/' ./launcher/build.gradle.kts

      - name: Publish to Maven Repository
        env:
          ORG_GRADLE_PROJECT_releasesUsername: ${{ secrets.MAVEN_USERNAME }}
          ORG_GRADLE_PROJECT_releasesPassword: ${{ secrets.MAVEN_PASSWORD }}
          ORG_GRADLE_PROJECT_snapshotsUsername: ${{ secrets.SNAPSHOTS_MAVEN_USERNAME }}
          ORG_GRADLE_PROJECT_snapshotsPassword: ${{ secrets.SNAPSHOTS_MAVEN_PASSWORD }}
        run: |
          if [ "${{ steps.repository.outputs.REPOSITORY }}" = "snapshots" ]; then
            ./gradlew publishMavenAllPlatformsPublicationToSnapshotsRepository --parallel
          else
            ./gradlew publishMavenAllPlatformsPublicationToReleasesRepository --parallel
          fi
